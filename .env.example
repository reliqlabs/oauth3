# Server
SERVER_ADDR=127.0.0.1:8080

# SQLite path (will be created automatically)
DATABASE_URL=oauth3.db

# JWT secret for signing access tokens (change in production)
JWT_SECRET=CHANGE_ME_DEVELOPMENT_SECRET

# OAuth2 provider configs are now stored in the database (table: oauth_providers).
# Insert rows via SQL after the service creates/migrates the database.
# See README.md for provider insert examples.
