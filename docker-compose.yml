services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: oauth3
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 10
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  simulator:
    image: phalanetwork/tappd-simulator:latest
    restart: unless-stopped
    volumes:
      - dstack-socket:/var/run
    ports:
      - "8090:8090"

  app:
    image: oauth3:nix
    restart: unless-stopped
    volumes:
      - dstack-socket:/var/run
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://postgres:postgres@db:5432/oauth3
      RUST_LOG: info,oauth3=debug
    extra_hosts:
      - "localhost:host-gateway"
    ports:
      - "8080:8080"

  dex:
    image: dexidp/dex:v2.41.1
    ports:
      - "5556:5556"
    volumes:
      - ./dex-config.yaml:/etc/dex/config.docker.yaml
      - dexdata:/var/dex
    command: dex serve /etc/dex/config.docker.yaml

volumes:
  pgdata:
  dexdata:
  dstack-socket:
